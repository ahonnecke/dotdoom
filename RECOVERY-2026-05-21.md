# Recovery Branch: from-nvme0n1p8-2026-05-21

This branch (`recovery/from-nvme0n1p8-2026-05-21`) carries my Doom Emacs
configuration work that existed on `nvme0n1p8` (pre-incident /home) but
diverged from `origin/main` because a post-incident recovery session
pushed reconstruction commits to origin without the full nvme0n1p8 history.

## Divergence picture

```
                  ───B───C───D   origin/main (3 commits authored post-incident
                /           ↑    by an LLM rebuild session — nerd-fonts fix,
─── ... ─── A ─                  dead-code cleanup, daemon hardening)
                \
                  ───E───...───Z   nvme0n1p8/main (46 commits of pre-incident
                              ↑    work: anvil.el MCP server, orchard milestones,
                              recovery/from-nvme0n1p8-2026-05-21
                              agent-shell health checks, claude vterm fixes,
                              notmuch transient, etc.)

A = last common ancestor
```

## What's in this branch (46 + 1 commits, vs origin/main)

### Headline features

- `feat: add anvil.el as MCP server for IDE context in LLM sessions`
- `feat(orchard): milestones, tiered caching, branch creation flow`
- `feat: add notmuch email, doom-dashboard, update keybindings`
- `feat(notmuch): transient menu, date-scoped searches, auto-refresh`
- `feat(agent-shell): add grab-url to open last URL from agent buffer`
- `feat(agent-shell): add health check and bounce for stuck sessions`
- `feat: add expand-region ts-mode support, jinx, whole-line-or-region, rainbow extensions`

### Fixes (orchard / claude-code / vterm focus)

- `fix(claude-code): point to fork with delete-window sole window fix`
- `fix(dispatch): load eagerly so emacsclient can resolve dispatch functions`
- `fix(vterm): fix terminal width at source — eliminate vterm-min-window-width race`
- `fix(orchard): sync transient menu with actual keymap, remove dead code`
- `fix(orchard): intercept claude-code--term-make to prevent window fights`
- `fix(claude): reset-window physically resizes window to trigger vterm pipeline`
- `fix(orchard): use claude --continue for automatic session resume`
- ... ~30 more fixes across orchard/claude-code/agent-shell

### Style + docs

- `style: complete docstring coverage — 914/914 defuns documented`
- `style: document all advice-add and add-hook call sites`
- `style: document all buffer-local variable sites`
- `style: LLM patterns compliance — lexical-binding, docstrings, gptel cleanup`
- `docs: add LLM elisp patterns guide and agent-shell smoke tests`
- Plus several more docs commits

### Final commit on top: WIP config-anvil.el

The pre-incident work tree had **one uncommitted change** in `config-anvil.el`
(3 lines added, 11 lines removed) — looks like an in-flight refactor of the
anvil integration config. Committed here as the final commit on the branch
so it doesn't get lost.

## What origin/main has that this branch doesn't (3 commits)

Authored by a post-incident reconstruction session — needs to be either
cherry-picked onto this branch OR resolved during merge:

- `d9dd9ae fix: install nerd fonts, drop +childframe, recover lost bindings`
- `c186fb5 chore: drop dead code & dangling .crewcapableai.main refs`
- `250559b fix: harden Emacs daemon against accidental death + WM-induced state corruption`

The "dead code & dangling .crewcapableai.main refs" commit may have removed
references that ALSO exist on this recovery branch — review carefully.

## How to merge

```bash
git fetch origin

# Option A — accept this branch's history, layer origin's 3 commits on top
git checkout recovery/from-nvme0n1p8-2026-05-21
git cherry-pick origin/main~2 origin/main~1 origin/main
# Resolve conflicts on a per-commit basis (the dead-code commit likely conflicts).
# Then force-push the recovery branch:
git push -f origin recovery/from-nvme0n1p8-2026-05-21
# Then on GitHub: open PR from recovery branch to main, merge.

# Option B — base main on this branch directly (rewrites origin/main history)
# Risky — only if no one else (i.e. no other machine, no other LLM session)
# has cloned origin/main:
git checkout main
git reset --hard recovery/from-nvme0n1p8-2026-05-21
git cherry-pick origin/main_PRE_RESET~2 origin/main_PRE_RESET~1 origin/main_PRE_RESET
# (save the SHAs of origin's 3 commits before reset)
git push -f origin main

# Option C — merge with explicit strategy (preserves both histories as merge commit):
git checkout main
git merge recovery/from-nvme0n1p8-2026-05-21 -m "Merge recovered pre-incident Doom Emacs config history"
# Resolve conflicts, push.
```

**Recommended: Option A.** Preserves origin's recent reconstruction work as
the top of history, keeps the recovery branch as a reviewable artifact.

## Tag

A tag `orchard-modular-v1.0` was also recovered from nvme0n1p8 (pointing
at commit `d110567`, mid-history of this recovery branch). Pushed alongside
the branch.

## Source / provenance

- Source: `/mnt/old_home/ahonnecke/.doom.d/` on nvme0n1p8 (pre-incident /home)
- Method: rsync of `.doom.d/.git/` into `/tmp/dotdoom.git-recovered/` →
  added as remote `nvme0n1p8` to live `~/src/dotdoom` clone → branched off
  `nvme0n1p8/main`
- The recovered `.git/` was full git history, including the WIP work-tree
  change which was applied as the final commit on this branch.
