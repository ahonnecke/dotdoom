# TODO

* DONE Cleanup (Dec 2025)
** DONE cleanup all the string manipulation bindings
   - config-inflection.el: transient menu (C-c i ?) shows all conversions with preview
   - Mode-aware cycling (C-c i i): Python=snake/CONST/Pascal, TS=camel/CONST/Pascal
** DONE indent tool binding
   - C-c < / C-c > with repeat mode (just press < or > to keep adjusting)
   - Mode-specific step: python-indent-offset, typescript-indent-level, etc.
   - C-c d = duplicate line/region (was crux-duplicate)
** DONE for all custom actions: make everything work on region or thing-at-point
   - String manipulation: already works on symbol-at-point
   - firefox-search: now works on region OR symbol/word at point
   - monet-explain/refactor: now work on region OR defun at point
   - Go-to actions: all work on appropriate thing-at-point

* DONE Add generic CLI modes (Dec 2025)
** DONE vercel - C-c V transient
** DONE supabase - C-c B transient
** DONE aws - C-c A transient

* DONE Enhance modes for crewcapable
** DONE C-c C prefix for crewcapable-services
** DONE Dev workflow integration

* DONE Meeting system (Dec 2025)
** DONE Replace standalone standup with generic meeting system
   - config-meeting.el: C-c M prefix
   - Per-occurrence files: ~/org/meetings/<type>/<date>.org
   - Meeting types: standup, psc-it-sync, psc-pm-sync
** DONE Standup sections: Done/Doing/Blockers/Questions/Agenda
** DONE Slack posting: C-c M s p

* TODO Calendar integration
** TODO Gmail calendar (read-only via org-caldav)
   - Query meeting times from calendar
   - Smart "next meeting" detection
** TODO Other calendars (Fastmail, Apple, personal Gmail)
   - Deferred - "whole project"

* TODO Other improvements
** TODO Tree-sitter: ensure all grammars installed
   - M-x treesit-install-all-grammars
** TODO Combobulate: verify working in ts-modes
