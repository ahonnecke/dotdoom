;;; config-org-download.el -*- lexical-binding: t; -*-

;; org-download: drag-and-drop/paste images into org buffers.
;; Not currently used — disabled to prevent void-function error in dired.
;; To re-enable: add (package! org-download) to packages.el and uncomment below.
;; (add-hook 'dired-mode-hook #'org-download-enable)
